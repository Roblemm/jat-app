package com.jat.app.entity;

// Status describes where an opportunity sits in the application pipeline.
public enum JobOpportunityStatus {
    SAVED,
    APPLIED,
    INTERVIEWING,
    OFFER,
    REJECTED,
    ARCHIVED
}
